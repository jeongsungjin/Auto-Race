<launch>
  <arg name="version" default="safe"/>
  <node name="lane_detection" pkg="lane_detection" type="lane_detection_drive.py" output="screen">
    <param name="version" value="$(arg version)"/>
  </node>
</launch>
